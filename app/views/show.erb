
<div class="container">
THis is the users prof page with their picture, name, username, and all posts
<% if current_user %>
  <% user = current_user %>

    <div class="list_skatelogs">

    </div>

    <div class="actions">
      <div class="btn_container">
        <a class='btn' href="/users/delet">delete a session</a>
      </div>
    </div>

    <div class="actions">
      <div class="btn_container">
        <a class='btn' href="/users/new">add a new sessions</a>
      </div>
    </div>

  <% end %>
</div>
